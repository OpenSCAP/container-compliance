machine:
  services:
    - docker
dependencies:
  pre:
    - sudo pip install docker-compose
  override:
    - make build
test:
  override:
    - make test
deployment:
  hub:
    branch: master
    commands:
      - curl --data "build=true" -X POST "$DOCKERHUB_AUTOBUILD_URL"
